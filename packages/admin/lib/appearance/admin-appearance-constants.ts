/** Framework-owned constants for the admin appearance system. */
export class AdminAppearanceConstants {
  /** Id of the framework's built-in default admin appearance (ships in packages/admin, never relocated). */
  static readonly DEFAULT_APPEARANCE_ID = 'default';
}
