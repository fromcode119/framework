/**
 * Fromcode React Bridge
 * Shares core framework components and hooks with plugins.
 */
export const { 
  Slot, 
  Override, 
  usePlugins, 
  useTranslation, 
  PluginsProvider, 
  getIcon, 
  createProxyIcon, 
  Icon 
} = window.Fromcode;
