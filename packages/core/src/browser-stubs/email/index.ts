// Barrel for the `@fromcode119/email` browser alias — every value core imports from it, as an empty class each.
export { EmailFactory } from '@core/browser-stubs/email/email-factory';
export { EmailManager } from '@core/browser-stubs/email/email-manager';
