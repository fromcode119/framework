/**
 * Browser-side stand-in for `EmailFactory` of `@fromcode119/email` — a server-only package that core modules
 * import; compiled for the browser too, where this name is never called. Empty on purpose.
 */
export class EmailFactory {}
