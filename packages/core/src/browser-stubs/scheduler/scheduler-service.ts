/**
 * Browser-side stand-in for `SchedulerService` of `@fromcode119/scheduler` — a server-only package that core modules
 * import; compiled for the browser too, where this name is never called. Empty on purpose.
 */
export class SchedulerService {}
