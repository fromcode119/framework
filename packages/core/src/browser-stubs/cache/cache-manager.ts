/**
 * Browser-side stand-in for `CacheManager` of `@fromcode119/cache` — a server-only package that core modules
 * import; compiled for the browser too, where this name is never called. Empty on purpose.
 */
export class CacheManager {}
