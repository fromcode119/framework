/**
 * Browser-side stand-in for `MediaManager` of `@fromcode119/media` — a server-only package that core modules
 * import; compiled for the browser too, where this name is never called. Empty on purpose.
 */
export class MediaManager {}
